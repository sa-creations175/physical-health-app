// Build 2.1 placeholder — the full cardio logger (date+time, type picker,
// duration nudge, intensity pills, notes, save) lands in step 2.1.4.
export default function LogCardio() {
  return (
    <div className="px-5 pt-8 pb-8">
      <p className="text-[12px] text-card-mute">Cardio logger coming up.</p>
    </div>
  );
}
